# ===========================================
# ThePrimeagen-inspired tmux config
# ===========================================

# Fix colors and enable true color
set -ga terminal-overrides ",screen-256color*:Tc"
set-option -g default-terminal "screen-256color"

# Allow extended keys (fixes Shift+Enter in Claude Code, neovim, etc.)
set -s extended-keys on
set -as terminal-features 'xterm*:extkeys'

# Use zsh
set-option -g default-shell /usr/bin/zsh

# No delay after pressing Escape (critical for vim/neovim)
set -s escape-time 0

# Keep default prefix C-b
set-option -g prefix C-b
bind-key C-b send-prefix

# Reload config with prefix + r
bind r source-file ~/.tmux.conf \; display "Config reloaded!"

# Windows start at 1, not 0
set -g base-index 1
set-window-option -g pane-base-index 1

# Renumber windows when one is closed
set -g renumber-windows on

# Vi mode for copy
set-window-option -g mode-keys vi
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xclip -in -selection clipboard'

# Vim-like pane switching
bind -r ^ last-window
bind -r k select-pane -U
bind -r j select-pane -D
bind -r h select-pane -L
bind -r l select-pane -R

# Quick pane resize (prefix + H/J/K/L)
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# Split panes with | and - (in current path)
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# New window in current path
bind c new-window -c "#{pane_current_path}"

# Mouse support
set -g mouse on

# Increase scrollback
set -g history-limit 50000

# --- Sessionizer (C-b f to fuzzy-switch projects) ---
bind-key -r f run-shell "tmux neww ~/.local/bin/tmux-sessionizer"

# ===========================================
# Status bar
# ===========================================
set -g status-position bottom
set -g status-interval 5
set -g status-left-length 40
set -g status-right-length 60
set -g status-style 'bg=#282828 fg=#d4be98'
set -g status-left '#[bg=#5CB8FF,fg=#282828,bold] #S #[bg=#282828] '
set -g status-right '#[fg=#a89984]%H:%M  %d-%b-%Y '
set -g window-status-format '#[fg=#7c6f64] #I:#W '
set -g window-status-current-format '#[bg=#3c3836,fg=#5CB8FF,bold] #I:#W '

# ===========================================
# Plugins (via TPM)
# ===========================================
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

# Resurrect settings
set -g @resurrect-capture-pane-contents 'on'

# Continuum — auto-save every 15 min, auto-restore on tmux start
set -g @continuum-restore 'on'
set -g @continuum-save-interval '15'

# Initialize TPM (keep this at the very bottom)
run '~/.tmux/plugins/tpm/tpm'
